# Hermes profile distribution manifest (generated by tools/build_team.py).
name: real-options-analyst
version: 1.2.0
description: "Values genuine embedded optionality (expansion, abandonment, delay, equity in troubled firms) and rejects the candidates that only look like options."
hermes_requires: ">=0.21.0"
author: "Kushal D'Souza (lyndonkl)"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "API key for the provider every model in config.yaml is routed through"
    required: true
