# Valuation critic (valuation team)

One member of the fifteen-Bot valuation team for Hermes Agent. Attacks the finished analysis for bias, double counting and unsupported growth, and raises findings for the owning stage to fix. Never edits the work it reviews.

This profile does not work alone: it receives jobs from `valuation-orchestrator` through Bot Mode's
`message_agent`, runs its stage, writes the artifacts the job names, and its final answer
returns to the orchestrator automatically. Chat with it directly to ask about its method.

## Install

Install the whole team from the repository root; the team only works together and only in
the Hermes desktop app with Bot Mode on (Settings → Plugins → Bots):

```bash
tools/install.sh --team valuation
python3 tools/team_models.py valuation --strong <model-id> --fast <model-id>   # optional model tiers
```

This member's tier is **strong**. `tools/install.sh` also writes the profile's Bot
metadata (title and role) so teammates see it in their roster.

## Skills bundled

| Skill | Category |
|---|---|
| `cost-of-capital-toolkit` | corporate-finance |
| `dcf-valuation-engine` | corporate-finance |
| `financial-statement-normalization` | corporate-finance |
| `monte-carlo-valuation` | corporate-finance |
| `option-valuation-toolkit` | corporate-finance |
| `project-investment-analysis` | corporate-finance |
| `relative-valuation-toolkit` | corporate-finance |
| `valuation-consistency-checks` | corporate-finance |
| `valuation-critic` | valuation-specialists |
| `valuation-playbooks` | corporate-finance |
| `valuation-red-team` | corporate-finance |

## Generated file

`SOUL.md` and this package are generated by `tools/build_team.py` from the stage brief
`packages/valuation-suite/skills/valuation-specialists/valuation-critic/SKILL.md` and the finance
skills under `packages/valuation-suite/skills/corporate-finance/`. Edit those and rebuild.
