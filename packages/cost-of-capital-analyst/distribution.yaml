# Hermes profile distribution manifest (generated by tools/build_team.py).
name: cost-of-capital-analyst
version: 1.3.0
description: "Estimates and defends the discount rate: riskfree rate in the mandate currency, operation-weighted equity risk premium, bottom-up beta, cost of debt, market-value weights."
hermes_requires: ">=0.21.0"
author: "Kushal D'Souza (lyndonkl)"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "API key for the provider every model in config.yaml is routed through"
    required: true
