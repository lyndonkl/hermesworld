# Hermes profile distribution manifest (generated by tools/build_team.py).
name: financial-data-collector
version: 1.3.0
description: "Gathers every input an analysis needs: filings, footnotes, market data, macro series and reference tables, and reports honestly what it could not find."
hermes_requires: ">=0.21.0"
author: "Kushal D'Souza (lyndonkl)"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "API key for the provider every model in config.yaml is routed through"
    required: true
