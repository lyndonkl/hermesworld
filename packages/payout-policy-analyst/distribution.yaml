# Hermes profile distribution manifest (generated by tools/build_team.py).
name: payout-policy-analyst
version: 1.3.0
description: "Judges whether the company returns the right amount of cash to its owners, and in the right form, against the free cash flow to equity it generates."
hermes_requires: ">=0.21.0"
author: "Kushal D'Souza (lyndonkl)"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "API key for the provider every model in config.yaml is routed through"
    required: true
